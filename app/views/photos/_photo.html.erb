<%#@photos.each do |p| %>

<%#= p.title %>
<%#= link_to image_tag(p.image.url(:thumb), class: 'media-object'), p.image.url %>


<%#end%>


<%= render template: 'photos/show', :locals => {:photo => photo} %>